describe('Example to see if tests works', () => {
    it('Should run the test', () => {
        expect(true).toBe(true);
    })
    test('Should run too', () => {
        const result:boolean = true;
        expect(result).toBe(true);
    })
})

